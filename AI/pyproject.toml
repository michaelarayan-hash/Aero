[project]
name = "aero-ai"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "torch",
    "torchvision",
    "onnxruntime",
    "numpy<2",
    "mavsdk>=2.0.0",
]

[dependency-groups]
dev = ["pytest>=8.0"]
