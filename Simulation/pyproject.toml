[project]
name = "aero-simulation"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "mavsdk>=2.0.0",
    "numpy<2",
    "opencv-contrib-python>=4.8,<4.10",
    "pupil-apriltags>=1.0.1",
]

[dependency-groups]
dev = ["pytest>=8.0"]
